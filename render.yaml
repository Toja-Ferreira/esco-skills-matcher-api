services:
  - type: web
    name: esco-backend
    runtime: docker
    dockerfile: Dockerfile
    env: python
    plan: standard
    port: 10000  # Render's required port
    envVars:
      - key: PORT
        value: 10000